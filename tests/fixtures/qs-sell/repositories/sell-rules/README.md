# qs/sell fixture — sell-rules

This is the synthetic `sell-rules` repository of the qs/sell fixture.
The fixture is **synthetic** and must not be confused with any real
private code.

## Files

- `adr/0001-qs-sell-auth.md` — rationale for the qs/sell endpoint.
- `rule/sell-rules.md` — sell rules.

The ADR matching is case-insensitive (P4): both `adr` and `ADR` are
matched by the v0.5.0 knowledge adapter.
